import React from "react";
import "./App.css";

/**
 * 
 * Contact HTML structure
 * 
 *	<li key={contact.id}>
		<p>{firstName} {lastName}</p>
		<p className={"details"}>
			<span>Age: <span className={"age"}>{age}</span></span>
			<span>Eye colour: <span className={"eyeColor"}>{eyeColor}</span></span>
		</p>
		<p>
			Email: 
			<span className={"email"}> 
				<a href={`mailto:${contact.email}`}>{contact.email}</a>
			</span>
		</p>
		<p className={"company"}>{contact.company}</p>
    </li>
 */

const App = () => {};

export default App;
